<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.teenyfin.teenymoney.domain.payment.mapper.PaymentMapper">

    <resultMap id="paymentResultMap" type="com.teenyfin.teenymoney.domain.payment.vo.PaymentVO">
        <id property="id" column="id"/>
        <result property="walletId" column="wallet_id"/>
        <result property="categoryId" column="category_id"/>
        <result property="orderId" column="order_id"/>
        <result property="idempotencyKey" column="idempotency_key"/>
        <result property="appliedPolicy" column="applied_policy"/>
        <result property="amount" column="amount"/>
        <result property="status" column="status"/>
        <result property="failureReason" column="failure_reason"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="countRecentTransactions" resultType="int">
        SELECT COUNT(*)
        FROM T_PAY_TRAN_L p
        JOIN T_WLT_BASE_M w ON p.wallet_id = w.id
        WHERE w.member_id = #{childId}
        AND p.category_id = #{categoryId}
        AND p.status = 'SUCCESS'
        AND p.created_at >= NOW() - INTERVAL 30 DAY
    </select>

    <select id="sumRecentTransactionAmount" resultType="java.lang.Long">
        SELECT COALESCE(SUM(p.amount), 0)
        FROM T_PAY_TRAN_L p
        JOIN T_WLT_BASE_M w ON p.wallet_id = w.id
        WHERE w.member_id = #{childId}
        AND p.category_id = #{categoryId}
        AND p.status = 'SUCCESS'
        AND p.created_at >= NOW() - INTERVAL 30 DAY
    </select>

    <insert id="insert" parameterType="com.teenyfin.teenymoney.domain.payment.vo.PaymentVO"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO T_PAY_TRAN_L (
        wallet_id,
        category_id,
        idempotency_key,
        applied_policy,
        amount,
        status,
        order_id
        )
        VALUES (
        #{walletId},
        #{categoryId},
        #{idempotencyKey},
        #{appliedPolicy},
        #{amount},
        #{status},
        #{orderId}
        )
    </insert>

    <select id="selectById" resultMap="paymentResultMap">
        SELECT
            id,
            wallet_id,
            category_id,
            order_id,
            idempotency_key,
            applied_policy,
            amount,
            status,
            failure_reason,
            created_at
        FROM T_PAY_TRAN_L
        WHERE id = #{id}
    </select>

    <select id="existsByOrderId" resultType="boolean">
        SELECT EXISTS (
            SELECT 1 FROM T_PAY_TRAN_L WHERE order_id = #{orderId}
        )
    </select>

    <select id="selectByIdempotencyKey" resultMap="paymentResultMap">
        SELECT
            id,
            wallet_id,
            category_id,
            order_id,
            idempotency_key,
            applied_policy,
            amount,
            status,
            failure_reason,
            created_at
        FROM T_PAY_TRAN_L
        WHERE idempotency_key = #{idempotencyKey}
    </select>

</mapper>